# 验收记录

## 2026-06-26

- 验收项：项目骨架创建。
- 状态：部分通过。
- 说明：目录结构、工程文件 plist 和 Swift 源文件语法已验证；完整 Xcode 构建因当前环境仅安装 Command Line Tools 而无法执行。

### 已执行检查

- `plutil -lint PictureSearch.xcodeproj/project.pbxproj`：通过。
- `swiftc -parse` 当前应用 Swift 源文件：通过。
- `xcodebuild -list -project PictureSearch.xcodeproj`：未通过，原因是当前开发者目录为 `/Library/Developer/CommandLineTools`，不是完整 Xcode。

## 2026-06-26

- 验收项：说明性文档中文化。
- 状态：通过。
- 说明：`AGENTS.md`、`PLAN.md`、`README.md` 和 `docs/` 下说明性内容已统一为中文；保留必要技术名词和命令名称。

## 2026-06-26

- 验收项：第 1 里程碑 PhotoKit 授权与真实图库读取。
- 状态：待人工验收。
- 已完成：权限说明页面、权限申请入口、授权状态处理、按时间范围读取图片资产、缩略图网格、创建时间、成功和失败数量。
- 待人工验证：系统授权弹窗、真实 Photos 缩略图显示、拒绝权限恢复指引、iCloud 图片不可用状态、Signing & Capabilities 和 entitlement 配置。
- 自动检查：`plutil` 通过，`swiftc -parse` 通过，`swiftc -typecheck` 通过。
- 未完成检查：完整 `xcodebuild` 构建未执行成功，原因是当前开发者目录为 `/Library/Developer/CommandLineTools`，不是完整 Xcode。

## 2026-06-26

- 验收项：拒绝 Photos 权限后的恢复入口。
- 状态：待人工复验。
- 说明：拒绝权限后按钮文案已改为“申请权限”；点击后应打开系统设置中的 Photos 权限页面。

## 2026-06-26

- 验收项：PhotoKit 读取范围选择。
- 状态：待人工验收。
- 说明：授权后可选择近一周、近一个月、近一年或全部图片；切换范围后应重新读取并更新缩略图、创建时间、成功数量和失败数量。

## 2026-06-26

- 验收项：第 2 里程碑 本地索引数据结构和任务状态。
- 状态：自动验收通过，待真实 App 人工复验。
- 已完成：资产索引记录模型、OCR/向量任务状态、SQLite 持久化、PhotoKit 资产同步、重复资产去重、索引状态页面、清除本地索引入口。
- 自动检查：`plutil` 通过，应用 Swift 源文件 `swiftc -typecheck` 通过，临时 `IndexStore` 可执行检查通过。
- Xcode 构建：使用 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild` 和 `/private/tmp/picture_search_derived` 作为 DerivedData 路径，`build` 通过。
- XCTest：`xcodebuild test` 已构建测试 bundle；因当前沙盒限制无法连接 `testmanagerd.control`，改用 Xcode 的 `xctest` 直接运行构建出的测试 bundle，7 个测试全部通过。
- 待真实 App 人工验证：重启后索引恢复、重复读取未变化计数、清除索引不影响 Photos 原图。
- 未完成范围：embedding、全文检索、向量检索、融合排序和大图预览仍未实现。

## 2026-06-26

- 验收项：第 3 里程碑 Apple Vision OCR。
- 状态：自动验收通过，待真实图库 OCR 样本人工验收。
- 已完成：本机 Vision OCR 服务、PhotoKit 缩放图片读取、OCR 状态写入、OCR 文本保存、失败类型记录、单张耗时记录、平均耗时统计、OCR 进度 UI、失败重试入口。
- 自动检查：`plutil` 通过，Xcode `build` 通过，直接运行测试 bundle 通过。
- XCTest：`xcodebuild test` 已构建测试 bundle；因当前沙盒限制无法连接 `testmanagerd.control`，改用 Xcode 的 `xctest` 直接运行构建出的测试 bundle，10 个测试全部通过。
- 待真实 App 人工验证：中文网页截图、英文文档截图、中英文混排界面、小字号截图、无文字生活照片、iCloud 图片不可用、失败重试和界面响应性。
- 未完成范围：OCR 文本检索、embedding、自然语言搜索、融合排序和大图预览仍未实现。

## 2026-06-26

- 验收项：第 4 里程碑 本地图文向量预留与安全降级。
- 状态：MVP 自动验收通过，真实视觉语义搜索增强项未验收。
- 已完成：模型调研和候选评估、本地模型适配层、模型 manifest 解析、外部模型引入原因/替代方案/影响评估校验、App bundle 中的 manifest 模板资源、运行时 manifest 自动加载和缺失降级、Application Support 本机模型包加载和缺失降级、manifest 缺失或模板占位诊断、模型文件大小元数据校验、模型和 tokenizer SHA-256 校验、tokenizer 起止 token ID 与 manifest 比对、结构化模型资源清单、资源实际大小与哈希审计、App 内可复制的 manifest 建议字段、本地模型包预检报告、候选 `.mlmodelc` Core ML 加载、运行时模型就绪 Core ML 加载检查、输入输出接口检查、输入 shape 与数据类型校验、研究/评估/非商业许可证限制校验、模型接入清单输出、模型缺失降级提示、模型就绪度报告、模型输入输出与预处理配置校验、图片 NCHW 归一化预处理、后台 utility 推理执行策略、本地 JSON BPE tokenizer 适配层、文本 token 输入构造、真实 Photos 验证样本描述符、样本描述 JSON 文档解析和隐私安全审计、App bundle 样本描述自动读取、模型与样本描述预检报告、App 内技术验证预检状态展示、当前模型版本技术验证门禁、App 内运行技术验证按钮和结果摘要展示、App 内视觉查询验证入口、样本描述模板、PhotoKit 验证样本图片加载桥接、图片编码器 Core ML 推理骨架、文本编码器 Core ML 推理骨架、模型输出向量解析、向量编码/解码、SQLite 向量持久化、模型版本记录、当前模型版本下旧向量需重建状态、交互式视觉索引批量上限、余弦相似度排序、视觉索引 UI 入口、mock 检索测试、带中文/英文/中英文混合覆盖检查、最小相似度差距、单样本实际相似度差距和单样本推理耗时记录的真实模型技术验证入口、包含模型接入清单、就绪诊断、相似度差距和耗时列的 Markdown 技术验证报告输出、技术验证报告和验证摘要本机留档、重启后按当前模型版本恢复验证门禁，以及 `docs/模型接入与验证指南.md` 和 `docs/视觉模型候选评估.md`。
- 自动检查：`plutil` 通过，Xcode `build` 通过，直接运行测试 bundle 通过。
- XCTest：`xcodebuild test` 已构建测试 bundle；因当前沙盒限制无法连接 `testmanagerd.control`，改用 Xcode 的 `xctest` 直接运行构建出的测试 bundle，57 个测试全部通过。
- MVP 完成依据：当前模型未配置或未通过当前模型版本技术验证时，视觉索引和视觉查询会被门禁阻止，不会生成伪视觉向量或伪视觉候选；后续第 5、6、7 里程碑可以先依赖 OCR、时间和基础类型信号推进。
- 增强项待验证：真实模型来源、许可证、文件大小、SHA-256、Core ML 输入输出、真实 tokenizer 与文本编码器匹配性、图片预处理、5 张真实 Photos 图片维度一致性、相关查询高于无关查询、中英文效果和处理耗时。
- 说明：`EmbeddingValidationService` 目前只使用 mock 向量测试验证流程本身；`EmbeddingModelManifest.example.json` 只是接入模板，当前仍缺少真实 `EmbeddingModelManifest.json`、`.mlmodelc` 和 tokenizer，不代表真实视觉语义搜索已经验收通过。新增的模型接入与验证指南只用于后续真实模型接入，不是 MVP 验收阻塞项。

## 2026-06-27

- 验收项：照片浏览区 UI 优化。
- 状态：自动检查通过，待真实 App 人工复验。
- 已完成：授权成功后的主界面继续保持左侧状态栏和右侧照片工作区；照片卡片元数据叠放在缩略图底部，减少单元格纵向高度；右侧照片区获得更高布局优先级。
- 自动检查：`plutil` 通过，Xcode `build` 通过，直接运行测试 bundle 通过。
- XCTest：`xcodebuild test` 已构建测试 bundle；因当前沙盒限制无法连接 `testmanagerd.control`，改用 Xcode 的 `xctest` 直接运行构建出的测试 bundle，34 个测试全部通过。
- 待人工验证：在 Xcode 中用真实图库、OCR 进行中和不同窗口尺寸检查是否能稳定显示多行照片，确认底部信息条不遮挡关键内容。

## 2026-06-28

- 验收项：照片浏览区密度优化。
- 状态：自动检查通过，待真实 App 人工复验。
- 已完成：进一步收紧主界面外边距、左侧状态栏宽度、照片工具栏高度和照片网格列宽，让右侧照片区在同一窗口中显示更多缩略图行。
- 自动检查：`plutil` 通过，Xcode `build` 通过，直接运行测试 bundle 通过。
- XCTest：`xcodebuild test` 已构建测试 bundle；因当前沙盒限制无法连接 `testmanagerd.control`，改用 Xcode 的 `xctest` 直接运行构建出的测试 bundle，42 个测试全部通过。
- 待人工验证：在 Xcode 中用真实图库、OCR 进行中和不同窗口尺寸检查照片网格是否明显比原界面展示更多行，确认时间和尺寸信息仍清晰可读。

## 2026-06-29

- 验收项：第 5 里程碑 查询解析、OCR 检索和融合排序。
- 状态：自动验收通过，待真实图库人工复验。
- 已完成：查询计划模型、结构化命中原因、结果置信度、时间解析、基础类型解析、OCR 关键词检索、OCR/时间/类型融合排序、低置信相近结果标记、纯画面语义查询降级、本地搜索面板和结果摘要展示。
- 自动检查：Xcode `build` 通过，直接运行测试 bundle 通过。
- XCTest：`xcodebuild test` 已构建测试 bundle；因当前沙盒限制无法连接 `testmanagerd.control`，改用 Xcode 的 `xctest` 直接运行构建出的测试 bundle，61 个测试全部通过。
- 已覆盖样例：“包含 Hermes 的截图”“去年夏天的截图”“2025 年 10 月的截图”“文档里的 Apollo”和“海边夕阳”视觉不可用降级。
- 待真实 App 人工验证：完成 OCR 后用真实 Photos 查询集检查 Top 结果、命中原因、低置信状态和索引未完成时的提示是否符合预期。
- 未完成范围：结果网格、大图预览、复制或拖拽复用、SQLite FTS、真实视觉语义信号参与排序仍属于后续里程碑或增强项。
