# 开发记录

## 2026-06-26

- 创建 macOS SwiftUI 项目骨架。
- 创建 `PictureSearch.xcodeproj`、应用入口、服务层占位类型、模型占位类型和 XCTest 入口。
- 创建项目计划、README、决策记录、已知问题和验收记录文档。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj`，工程文件 plist 校验通过。
- 已运行 `swiftc -parse` 校验当前应用 Swift 源文件，语法解析通过。
- 尝试运行 `xcodebuild -list -project PictureSearch.xcodeproj`，但当前环境只配置了 Command Line Tools，缺少完整 Xcode，无法执行 Xcode 构建检查。
- 当前尚未实现 PhotoKit 授权、图库读取、OCR、embedding、索引或自然语言搜索。

## 2026-06-26

- 将 `AGENTS.md`、`PLAN.md`、`README.md` 和用户可见占位文案统一调整为中文。
- 保留 macOS、PhotoKit、OCR、embedding、Xcode 等必要技术名词。

## 2026-06-26

- 将 8 个可独立验收的开发里程碑写入 `PLAN.md`。
- 实现第 1 里程碑：PhotoKit 授权与真实图库读取。
- 新增 Photos 权限状态模型、资产摘要模型和缩略图加载结果模型。
- `PhotoLibraryService` 现在只通过 PhotoKit 请求权限，按用户选择的时间范围读取图片资产并请求缩略图。
- 主界面现在展示用途和隐私说明、权限状态、真实图库缩略图网格、创建时间、成功数量和失败数量。
- 新增授权状态、资产摘要和加载统计的基础单元测试。
- 本阶段未实现 OCR、embedding、索引数据库、自然语言搜索和大图预览。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，工程文件和 entitlements 校验通过。
- 已运行 `swiftc -parse` 校验当前应用 Swift 源文件，语法解析通过。
- 已运行 `swiftc -typecheck` 校验当前应用 Swift 源文件，类型检查通过；为了适配当前 Command Line Tools 环境，已将 SwiftUI `#Preview` 宏替换为传统 `PreviewProvider`。
- 已尝试运行 `xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' build`，但当前环境只配置了 Command Line Tools，缺少完整 Xcode，无法执行完整构建。

### 第 1 里程碑人工操作

- 在 Xcode 中确认 `NSPhotoLibraryUsageDescription` 已写入 Info 配置。
- 在 Xcode 中确认 Signing & Capabilities 包含 App Sandbox 和 Photos Library 访问 entitlement。
- 运行 App 后分别测试未决定、同意、拒绝、撤销权限和 iCloud 图片不可用场景。
- 保留权限说明页、系统授权弹窗、授权后网格、拒绝权限状态和首次图片读取失败状态截图。

## 2026-06-26

- 根据人工验收反馈，调整拒绝 Photos 权限后的恢复入口。
- 拒绝、受限或未知权限状态下，主按钮显示为“申请权限”。
- 用户点击“申请权限”时打开系统设置的 Photos 隐私页面，并提示回到 App 后点击“刷新状态”。
- 保留未决定状态下的系统授权弹窗申请流程；不假装 macOS 能在拒绝后重复弹出授权框。

## 2026-06-26

- 将第 1 里程碑的读取范围从固定数量限制调整为可选范围：近一周、近一个月、近一年、全部。
- `PhotoLibraryService` 改为按所选时间范围通过 PhotoKit 查询图片资产。
- 图库页面新增分段选择器，切换范围后自动重新读取缩略图。
- 同步更新 `PLAN.md`、`README.md` 和验收记录中的阶段说明。

## 2026-06-26

- 执行第 2 里程碑：本地索引数据结构和任务状态。
- 新增 `AssetIndexRecord`、`IndexTaskStatus`、`IndexStatusSummary` 和 `IndexSyncResult`，覆盖资产 ID、创建时间、媒体类型、媒体子类型、尺寸、OCR 文本、OCR 状态、图片向量、向量状态、模型版本、最近索引时间和失败原因。
- 使用系统 SQLite 实现 `IndexStore`，数据库默认写入 Application Support 下的 `PictureSearch/Index.sqlite`。
- PhotoKit 读取资产后会把资产摘要同步到本地索引；同一资产未变化时计为 unchanged，不重复创建记录。
- 主界面新增本地索引状态区，展示记录数、新增/更新/未变化数量，以及 OCR 和向量任务的 pending/processing/ready/failed 状态。
- 新增“清除本地索引”入口，只删除本地派生索引记录，不修改或删除 Photos 原图。
- 新增索引记录默认状态、SQLite 持久化、去重和清除逻辑测试。
- 本阶段未实现 OCR、embedding 生成、全文检索、向量检索或融合排序。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，工程文件和 entitlements 校验通过。
- 已运行 `swiftc -typecheck` 校验当前应用 Swift 源文件，类型检查通过。
- 已运行临时 `IndexStore` 可执行检查，验证 SQLite 插入、重复同步去重、状态统计和清除逻辑通过。
- 已使用 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build` 完成正式构建，结果通过。
- 已使用单独 DerivedData 路径运行 `xcodebuild test`；测试 bundle 构建成功，但测试运行阶段被当前沙盒限制阻止连接 `testmanagerd.control`。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，7 个 XCTest 全部通过。

## 2026-06-26

- 执行第 3 里程碑：Apple Vision OCR。
- 新增 `OCRServicing` 协议和 `OCRService` 实现，使用 Apple Vision `VNRecognizeTextRequest` 在本机识别图片文字，优先配置中文简体和英文识别语言。
- 新增 `OCRModels.swift`，记录 OCR 识别结果、失败类型、平均耗时和失败类型统计，并提供文本归一化逻辑。
- `PhotoLibraryService` 新增通过 PhotoKit 按资产 ID 获取图片资产、为 OCR 请求缩放后图片并转换为 `CGImage` 的方法；仍不直接访问 Photos Library 文件包。
- `IndexStore` 扩展 OCR 状态写入、失败类型、单张耗时、OCR 候选查询和性能统计；已有数据库会自动补充 `ocrDurationSeconds` 和 `ocrFailureType` 字段。
- 主界面索引状态区新增“开始 OCR”和“重试失败”入口，展示 OCR 进度、平均耗时和主要失败类型。
- OCR 调度使用后台 `Task.detached` 执行，当前逐张串行处理，避免大量图片同时解码；单张失败会记录失败类型和原因，并继续处理后续图片。
- 新增 OCR 文本归一化、`OCRService` mock、OCR 状态流转、失败类型保存、平均耗时统计和重试候选选择测试。
- 本阶段未实现 OCR 文本检索、自然语言搜索、embedding 生成、融合排序或大图预览。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，工程文件和 entitlements 校验通过。
- 已使用 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build` 完成正式构建，结果通过。
- 已使用单独 DerivedData 路径运行 `xcodebuild test`；测试 bundle 构建成功，但测试运行阶段仍被当前沙盒限制阻止连接 `testmanagerd.control`。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，10 个 XCTest 全部通过。

## 2026-06-26

- 推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 已完成模型调研并新增 `docs/模型调研.md`；Apple MobileCLIP 代码为 MIT，但模型权重限制为研究用途，因此当前不作为默认内置模型；OpenAI CLIP 为优先候选，但需要转换为 Core ML 并确认 tokenizer 与预处理。
- 新增 `EmbeddingServicing` 协议、`EmbeddingModelInfo`、`EmbeddingVector`、`EmbeddingFailureType` 和本地 Core ML CLIP 适配层占位。
- 默认模型版本为 `local-clip-unconfigured`，默认查找 `CLIPImageEncoder.mlmodelc` 和 `CLIPTextEncoder.mlmodelc`；模型缺失时只给出明确提示，不联网下载、不上传图片、不使用人工标签替代视觉搜索。
- 扩展 `IndexStore`：图片向量可写入 SQLite，记录 `modelVersion`、embedding 状态、embedding 耗时和失败类型；支持按模型版本筛选可用向量并按余弦相似度排序。
- 扩展 `SearchService`：新增基于文本向量和图片向量的视觉语义候选检索入口。
- 主界面索引状态区新增“开始视觉索引”和“重试视觉失败”入口；模型未配置时展示恢复提示，不会消耗 pending 队列。
- 新增向量编码/解码、余弦相似度、embedding 持久化、模型版本记录和视觉检索 mock 测试。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，13 个 XCTest 全部通过。

### 第 4 里程碑当前限制

- 尚未加入真实 Core ML 图文双塔模型，因此未完成 5 张真实图片的图文向量维度一致验证，也未完成中文、英文和中英文查询效果对比。
- `EmbeddingService` 的真实 Core ML 输入输出适配仍需在确定模型文件、tokenizer 和预处理规则后实现。
- 本阶段没有把视觉语义搜索标记为最终完成；当前仅完成可测试的本地向量基础设施和模型缺失降级。

### 第 3 里程碑人工操作

- 在 Xcode 中运行 App，授权并读取真实图库后点击“开始 OCR”。
- 使用中文网页截图、英文文档截图、中英文混排界面、小字号截图和无文字生活照片检查 OCR 文本是否足以支持后续文字找图。
- 观察 OCR 处理时界面是否仍可滚动和响应，记录平均耗时和主要失败类型。
- 对 iCloud 原图不可用或图片读取失败样本，确认失败数量和失败原因可见，并可通过“重试失败”重新处理。

## 2026-06-26

- 根据真实 OCR 运行界面反馈，优化主界面布局。
- 授权说明和本地索引状态从纵向堆叠调整为左侧状态栏，照片网格调整为右侧主工作区，减少状态信息对照片区域高度的挤压。
- 压缩授权卡片和索引卡片内边距，索引状态改为更适合窄侧栏的两列指标和纵向任务状态。
- 照片网格区域现在可占据窗口主要高度，便于一次查看更多行缩略图。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，10 个 XCTest 全部通过。

## 2026-06-26

- 根据真实使用反馈继续优化照片浏览区域。
- 授权后的主界面不再让标题占用右侧照片工作区上方空间，标题、授权说明、索引状态和运行提示集中在左侧可滚动状态栏。
- 右侧照片区域只保留范围选择、读取按钮、统计信息和缩略图网格，减少顶部占用。
- 缩略图网格从较大的 150-190 点自适应单元调整为 128-168 点自适应单元，并压缩单元文字间距，便于在同一窗口高度下显示更多行照片。
- 本次没有实现大图预览、搜索结果页或照片复用能力；点击放大仍属于后续第 6 里程碑范围。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，13 个 XCTest 全部通过。

## 2026-06-26

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingValidationService`，作为真实 Core ML 图文模型接入后的技术验证入口。
- 验证器要求至少 5 张测试图片，并检查图片向量、相关文本向量和无关文本向量维度一致。
- 验证器会比较相关查询与无关查询的余弦相似度，默认要求相关查询至少高出 `0.05`，并记录中文、英文和中英文混合查询覆盖情况。
- 新增验证器测试：样本不足必须失败；5 张 mock 样本覆盖中文、英文和中英文混合查询时，相关查询分数必须高于无关查询。
- 本次仍未加入真实 Core ML CLIP 模型，也未实现 tokenizer 或图片预处理；mock 测试只证明验证流程可运行，不证明真实语义效果通过。
- 模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索仍未标记完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，15 个 XCTest 全部通过。

## 2026-06-26

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 扩展 `EmbeddingModelInfo`，加入可选的 `EmbeddingModelConfiguration`，用于记录真实模型的图片输入输出名、文本输入输出名、tokenizer 资源、图片输入尺寸和归一化参数。
- 新增 `EmbeddingModelReadinessReport`，把模型文件存在、tokenizer 存在和配置完整性分开检查。
- `EmbeddingService.validateModelAvailability()` 现在只有在图片模型、文本模型、tokenizer 和配置全部满足时才通过；避免只放入 `.mlmodelc` 文件就误判视觉模型可用。
- 新增测试覆盖默认模型缺失和配置缺失提示，以及配置字段校验。
- 本次没有加入外部模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 推理、tokenizer 实现、图片预处理和真实 5 张图片语义效果验证仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，17 个 XCTest 全部通过。

## 2026-06-26

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增图片预处理路径：将 `CGImage` 中心裁剪为正方形，缩放到配置尺寸，并按 RGB 三通道 mean/std 生成 NCHW `MLMultiArray`。
- 新增模型输出解析：支持从 Core ML `MLMultiArray` 输出中拉平成 `EmbeddingVector`。
- `EmbeddingService.encodeImage(_:)` 现在在模型配置完整时会使用后台 `Task.detached(priority: .utility)` 加载图片编码器 `.mlmodelc`，执行 Core ML prediction，并读取配置指定的输出向量。
- `EmbeddingService.encodeText(_:)` 仍只做模型可用性检查，随后明确返回 tokenizer 和文本输入适配未实现；不会用人工标签、OCR 文本或文件名冒充文本向量。
- 新增图片预处理和模型输出解析测试。
- 本次没有加入外部模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、tokenizer、文本编码器输入适配和 5 张真实图片语义效果验证仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，19 个 XCTest 全部通过。

## 2026-06-26

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 将视觉模型就绪度从单段错误提示扩展为结构化诊断：分别记录缺少的图片模型、文本模型、tokenizer、输入输出配置和预处理配置问题。
- `EmbeddingModelReadinessReport` 新增缺失项列表、诊断行和恢复提示，便于后续加入真实 Core ML 模型后逐项排查。
- `PhotoLibraryViewModel` 和索引状态面板现在展示模型版本、来源、许可证、缺少资源和配置问题；模型未就绪时仍不会上传图片，也不会用人工标签替代视觉搜索。
- 新增测试覆盖模型诊断列表，确保缺失资源和配置问题不会混在一起误判。
- 本次没有加入外部模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、tokenizer、文本编码器输入适配和 5 张真实图片语义效果验证仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，20 个 XCTest 全部通过。

## 2026-06-26

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 扩展 `EmbeddingModelConfiguration`，新增文本 token 序列长度和 padding token ID，用于适配固定长度的 Core ML 文本编码器输入。
- 新增 `EmbeddingTextTokenizing` 协议、默认不可用 tokenizer 和 `EmbeddingTextInputBuilder`，将真实 tokenizer 产出的 token ID 构造成 `MLMultiArray`。
- `EmbeddingService.encodeText(_:)` 现在在模型与配置就绪后会在后台 `Task.detached(priority: .utility)` 中加载文本编码器 `.mlmodelc`，构造 token 输入，执行 Core ML prediction，并解析文本向量输出。
- 默认 tokenizer 仍明确返回失败；当前不会用人工标签、OCR 文本、文件名或关键词冒充文本向量。
- 新增测试覆盖文本序列长度配置校验、token padding 和超长 token 序列失败。
- 本次没有加入外部模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、真实 tokenizer 和 5 张真实图片语义效果验证仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，22 个 XCTest 全部通过。

## 2026-06-26

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingModelManifest`，支持用本地 JSON 描述模型版本、来源、许可证、图片模型名、文本模型名、tokenizer 资源和完整 `EmbeddingModelConfiguration`。
- `EmbeddingService` 新增通过 manifest 初始化的入口，后续真实模型打包后可以用 manifest 创建模型信息，不需要把模型来源、许可证和预处理参数散落在代码里。
- manifest 校验会检查模型版本、来源、许可证、图片模型文件名、文本模型文件名以及输入输出和预处理配置，避免只加入 `.mlmodelc` 文件就误判模型可用。
- 新增测试覆盖 manifest JSON 解析、manifest 到 `EmbeddingModelInfo` 的转换，以及缺失元数据的校验。
- 本次没有加入外部模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`，测试 manifest 仅用于解析流程，不包含真实模型文件。
- 真实 Core ML 图文双塔模型、真实 tokenizer 和 5 张真实图片语义效果验证仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，24 个 XCTest 全部通过。

## 2026-06-26

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 增强 `EmbeddingValidationService` 的真实模型技术验证报告，新增必需语言集合、已覆盖语言和缺失语言统计。
- 技术验证现在默认要求样本覆盖中文、英文和中英文混合查询；即使样本数量足够、相似度通过，只要缺少任一语言覆盖，报告也不会通过。
- 新增测试覆盖“5 张样本全部为中文时必须失败，并报告缺少英文和中英文混合查询”。
- 本次没有加入外部模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、真实 tokenizer 和 5 张真实图片语义效果验证仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，25 个 XCTest 全部通过。

## 2026-06-26

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `PictureSearch/Resources/EmbeddingModelManifest.example.json`，作为真实 Core ML 图文双塔模型接入前的本地 manifest 模板。
- 模板记录模型版本、来源、许可证、图片模型名、文本模型名、tokenizer 资源、文本序列长度、图片输入尺寸和 RGB 归一化参数；模板内容明确标注为未验证配置，不代表真实模型可用。
- 为 App target 新增资源复制阶段，确保 manifest 模板会打包到 App bundle，后续真实模型接入时可以沿用同一资源路径。
- 新增 XCTest 覆盖 manifest 模板解析和字段约束，防止模板缺少模型接入所需的关键字段。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过，并确认 manifest 模板被复制到 App bundle。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，26 个 XCTest 全部通过。

## 2026-06-26

- 根据真实使用反馈优化当前主界面布局：授权成功后改为左侧状态栏和右侧照片工作区，避免本地索引与 OCR 状态区把照片网格挤到页面底部。
- 左侧状态栏承载标题、权限说明、本地索引、OCR 和视觉索引状态，并可独立滚动；右侧照片网格占据主要可视区域。
- 收紧侧栏宽度，并将照片网格缩略图改为更紧凑的自适应尺寸，让同一屏能看到更多行图片。
- 调整照片区域顶部工具栏，在空间不足时自动换行，避免读取范围选择器和重新读取按钮压缩网格。
- 本次只调整 SwiftUI 布局，不改变 PhotoKit 读取方式、Photos 只读边界、OCR 处理逻辑、索引数据结构或视觉模型状态。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，25 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingValidationSampleDescriptorBundleResult`，让 App bundle 中的 `EmbeddingValidationSamples.json` 同时提供可审计摘要和运行时样本描述，避免预检能读到样本但运行验证时没有样本数据。
- `PhotoLibraryViewModel` 新增 App 内技术验证状态：运行中状态、验证提示和 `EmbeddingValidationReport` 结果。
- 视觉语义索引详情区新增“运行技术验证”按钮；预检通过后会通过 PhotoKit 读取样本图片，调用 `EmbeddingValidationService` 运行本地图文向量验证，并在界面中展示模型版本、语言覆盖、单样本分数、耗时和失败原因摘要。
- 技术验证样本读取失败时只展示样本 ID 和失败原因，不展示 Photos 私有资产标识。
- 新增测试覆盖 bundle 样本描述在运行时保留文档，同时隐私安全摘要不泄露 `assetLocalIdentifier`。
- 本次没有加入真实 Core ML 模型、外部依赖、网络能力或人工标签替代逻辑；模型版本仍为 `local-clip-unconfigured`。真实 5 张 Photos 样本、真实 tokenizer 匹配和中英文语义效果仍未完成验收，视觉语义搜索不能标记为完成。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，47 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingModelPackagedResource` 和 `EmbeddingModelResourceAudit.packagedResources`，结构化记录图片模型、文本模型和 tokenizer 是否已打包、实际大小、manifest 记录大小和资源问题。
- `EmbeddingModelReadinessReport` 现在携带模型资源打包清单；视觉语义索引详情区会展示“模型资源打包清单”，便于真实模型加入工程后确认资源是否随 App bundle 可见。
- 资源清单不包含本机路径和 Photos 信息，只展示资源名、大小和诊断；它只用于模型接入验收，不代表模型语义效果已经通过。
- 新增测试覆盖未配置模型时资源清单显示 `.mlmodelc` 未打包，以及临时模型/tokenizer 文件存在时能够显示实际大小并识别 manifest 大小不一致。
- 本次没有加入真实 Core ML 模型、外部依赖、网络能力或人工标签替代逻辑；模型版本仍为 `local-clip-unconfigured`。真实 5 张 Photos 样本、真实 tokenizer 匹配和中英文语义效果仍未完成验收，视觉语义搜索不能标记为完成。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，47 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `VisualSearchVerificationView`，在左侧状态栏提供“视觉查询验证”入口；该入口用于验证文本查询向量检索本地图片向量的链路，不替代后续完整搜索页。
- `PhotoLibraryViewModel` 新增视觉查询状态、查询文本、结果列表和后台执行方法；执行时会先检查 Photos 权限、本地索引、视觉模型就绪状态和 ready 向量数量。
- 视觉查询会在后台任务中调用 `SearchService.visualSearch`，由本地文本编码器生成查询向量，再检索 SQLite 中当前模型版本的 ready 图片向量；结果展示媒体子类型、创建时间、尺寸和真实视觉相似度说明。
- `SearchService.visualSearch` 现在会对空查询和无效 limit 直接返回空结果，避免无意义地调用文本编码器。
- 新增测试覆盖空视觉查询和无效 limit 不调用文本编码器、不返回伪结果。
- 本次没有加入真实 Core ML 模型、外部依赖、网络能力或人工标签替代逻辑；模型版本仍为 `local-clip-unconfigured`。真实 5 张 Photos 样本、真实 tokenizer 匹配和中英文语义效果仍未完成验收，视觉语义搜索不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，48 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingBatchPolicy`，当前交互式视觉索引默认每次最多处理 50 张候选图片，避免真实 Core ML 模型接入后一次性批量推理过多图片导致内存和响应风险。
- `PhotoLibraryViewModel` 的视觉索引调度现在只处理当前批次；若候选数量超过批量上限，剩余图片会保留为 `pending` 或需重建状态，用户可再次启动视觉索引继续处理。
- 视觉索引进度和完成提示现在会说明本批处理数量和候选总数，避免把部分批次误描述为全量完成。
- 新增测试覆盖批量策略的数量上限、顺序保留、零上限保护和提示文案。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、真实 tokenizer、5 张真实 Photos 图片维度一致验证、中文/英文/中英文效果对比和处理耗时验收仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，38 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 扩展 `EmbeddingValidationReport`，真实模型技术验证报告现在记录单张样本的图片编码耗时、相关文本编码耗时、无关文本编码耗时、总耗时和平均单样本耗时。
- 验证报告的 Markdown 表格新增耗时列，便于真实模型接入后把模型效果、向量维度、相似度差距和本机推理耗时放在同一份验收证据中。
- `EmbeddingValidationService` 在成功、相似度未达标、维度不一致和编码失败路径都会尽量保留已完成阶段的耗时记录。
- 新增测试覆盖验证报告耗时字段、Markdown 表格列和失败文案转义。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、真实 tokenizer、5 张真实 Photos 图片维度一致验证、中文/英文/中英文效果对比和真实处理耗时验收仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 首次构建失败于 `EmbeddingValidation.swift` 的长 Markdown 表格表达式，根因是 Swift 编译器类型检查超时；已拆分为显式局部变量和 `markdownTableRow(for:)` 后重新构建。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，38 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingValidationPreflightReport`，用于在真实模型技术验证前合并模型就绪诊断和样本描述审计。
- 预检报告会判断是否可以进入 PhotoKit 样本读取和 `EmbeddingValidationService` 技术验证；模型未就绪或样本不足、语言覆盖不足时会给出下一步恢复动作。
- 预检报告的摘要和 Markdown 输出只包含样本 ID、数量、语言覆盖和问题原因，不写入 Photos `assetLocalIdentifier`，避免把私有图库标识带入验收文档。
- 新增测试覆盖模型未就绪、样本不足、语言缺失、可继续验证和隐私不泄露路径。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、真实 tokenizer、5 张真实 Photos 图片维度一致验证、中文/英文/中英文效果对比和真实处理耗时验收仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，40 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingModelManifest` 新增许可证限制校验：manifest 中如果出现 `research only`、`research use only`、`non-commercial`、`academic use only`、`evaluation only`、`仅研究`、`研究用途`、`非商业`、`仅评估` 等限制，会被记录为 manifest 校验问题。
- 受限许可证模型不会被 `EmbeddingService.modelReadinessReport()` 判定为就绪，`validateModelAvailability()` 会失败，避免把研究用途或非商业权重误作为默认可用模型。
- 新增测试覆盖受限许可证被拒绝，以及 `MIT` 等宽松许可证文本不会被误判。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、真实 tokenizer、5 张真实 Photos 图片维度一致验证、中文/英文/中英文效果对比和真实处理耗时验收仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，42 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `PhotoLibraryViewModel` 现在维护 `EmbeddingValidationPreflightReport`，把当前本地模型就绪诊断和真实样本描述审计合并成 App 可展示的技术验证预检状态。
- 视觉语义索引详情区新增“技术验证预检”块，展示模型是否就绪、样本数量、语言覆盖、缺失语言、样本问题和下一步恢复动作；默认没有真实样本描述时会明确显示需要至少 5 张样本，并覆盖中文、英文和中英文混合查询。
- 新增 `EmbeddingValidationSampleDescriptorDocument.empty`，用于默认未配置样本时生成可审计的空样本状态。
- 新增 XCTest 覆盖空样本描述下的预检行为：即使模型 mock 就绪，没有真实样本也不能继续 PhotoKit 样本读取和技术验证；摘要和 Markdown 报告不得包含 Photos 私有资产 ID。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、真实 tokenizer、5 张真实 Photos 图片维度一致验证、中文/英文/中英文效果对比和真实处理耗时验收仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，43 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingValidationSampleDescriptorDocument` 新增 `bundledAudit`，App 可从 bundle 中读取 `EmbeddingValidationSamples.json` 作为真实 Photos 技术验证样本描述。
- 如果 `EmbeddingValidationSamples.json` 缺失或解析失败，预检会生成隐私安全的样本问题，提示基于 `docs/视觉模型验证样本.example.json` 填写真实样本并加入工程资源；诊断不会输出 JSON 内容或 Photos `assetLocalIdentifier`。
- `PhotoLibraryViewModel` 默认改为使用 bundled 样本审计结果，后续只要把真实样本描述作为资源加入 App，视觉语义索引详情区的技术验证预检就会自动反映样本数量、语言覆盖和解析状态。
- 新增 XCTest 覆盖 bundled 样本描述缺失、合法样本可通过、坏 JSON 不泄露私有资产 ID 三种路径。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`。
- 真实 Core ML 图文双塔模型、真实 tokenizer、5 张真实 Photos 图片维度一致验证、中文/英文/中英文效果对比和真实处理耗时验收仍未完成，视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，46 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingService` 新增 `bundledOrUnconfigured` 入口，App 启动时会优先尝试从 bundle 读取真实 `EmbeddingModelManifest.json`；如果缺少该文件，则安全降级到 `local-clip-unconfigured`。
- `PhotoLibraryViewModel` 默认改用运行时 manifest 加载入口，为后续加入真实本地 Core ML 图文双塔模型预留产品路径。
- `EmbeddingModelManifest` 新增模板占位识别，发现 `local-clip-template`、来源/许可证仍含“替换”或说明为未验证模板时，会把 manifest 标记为配置问题，避免误把 example 模板当成真实模型。
- 新增测试覆盖缺少 bundled manifest 时必须回退到 `local-clip-unconfigured`，以及 example manifest 必须被识别为模板占位。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，27 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingModelManifest` 新增 `imageModelSHA256`、`textModelSHA256` 和 `tokenizerSHA256` 字段；真实 manifest 缺少哈希或哈希格式不正确时会进入模型配置问题，模板 manifest 仍会被拒绝。
- `EmbeddingModelResourceAudit` 新增本机 SHA-256 计算和校验能力；对 `.mlmodelc` 目录会按排序后的相对路径和文件内容生成确定性哈希，对 tokenizer 文件直接计算内容哈希。
- 模型资源打包清单现在展示 SHA-256 是否匹配；模型就绪报告会把大小不一致和哈希不一致都纳入配置问题，避免真实模型文件被误替换、拷贝不完整或 tokenizer 指错时仍进入可用状态。
- 更新 `EmbeddingModelManifest.example.json` 和 `docs/模型接入与验证指南.md`，明确真实模型接入时必须记录大小和 SHA-256。
- 新增和更新 XCTest 覆盖 manifest 哈希字段解析、模板缺少哈希提示、`.mlmodelc` 目录哈希、tokenizer 哈希，以及哈希不一致时的中文诊断。
- 本次没有加入真实 Core ML 模型、外部模型文件、网络能力或人工标签替代逻辑；当前模型版本仍为 `local-clip-unconfigured`。真实 5 张 Photos 样本、中英文效果和处理耗时仍需接入真实模型后人工验证。
- 首次构建发现 `sha256ValidationIssues` 被放入错误的许可证校验作用域，已按构建错误修复；首次直接运行测试发现 tokenizer 缺少哈希的中文提示缺少空格，已修正为“缺少 tokenizer SHA-256”。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。`EmbeddingModelManifest.example.json` 是 JSON，不用 `plutil` 作为 plist 校验；其解析由 XCTest 覆盖。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，49 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingValidationReportStore`，App 内运行真实模型技术验证后会把 `EmbeddingValidationReport.md` 保存到本机 Application Support 的 `PictureSearch` 目录，便于保留模型版本、接入清单、就绪诊断、样本分数、失败原因和耗时记录。
- `PhotoLibraryViewModel` 现在会在技术验证完成后写入报告，并在界面消息中显示保存路径或保存失败原因；报告仍只记录样本 ID 和验证结果，不保存图片内容，也不应写入 Photos 私有资产标识。
- 新增 XCTest 覆盖报告目录创建、Markdown 写入、模型版本与结论留档，以及不把私有资产 ID 写入报告。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`。真实 5 张 Photos 样本、中英文效果和处理耗时仍需接入真实模型后人工验证。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，49 个 XCTest 全部通过。

## 2026-06-28

- 根据真实使用反馈继续优化照片浏览区，目标是减少上方和侧边信息对照片网格的挤占，让授权成功后同一屏能够看到更多图片行。
- 授权成功后的主窗口外边距、最小尺寸和左侧状态栏宽度进一步收紧；照片网格继续作为右侧主工作区并保持更高布局优先级。
- 照片网格自适应列宽从 `126...168pt` 调整为 `112...144pt`，工具栏高度和标题字号同步压缩，图片底部信息条高度收紧到 48pt。
- 本次只修改 SwiftUI 布局和说明文档，不改变 PhotoKit 读取范围、Photos 只读边界、OCR 管线、索引数据结构或视觉模型能力。
- 点击图片放大预览仍未实现，属于后续“搜索界面、结果解释和图片预览”里程碑，不能标记为当前已完成能力。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，42 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingModelResourceAudit`，用于递归计算 `.mlmodelc` 目录和 tokenizer 文件的实际大小，并与 `EmbeddingModelManifest` 中记录的文件大小对比。
- 模型就绪报告现在会把打包资源实际大小与 manifest 记录不一致的问题纳入配置问题，避免真实模型文件被替换、转换产物不一致或 manifest 记录错误时仍误判为可验收。
- `EmbeddingModelReadinessReport.isReady` 现在要求 `manifestIssue == nil`，确保 manifest 缺失、解析失败或模板占位本身会阻止模型进入就绪状态。
- 新增 XCTest 覆盖 manifest 问题阻止就绪、`.mlmodelc` 目录递归大小计算、tokenizer 文件大小校验，以及大小不一致时的中文诊断。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，29 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingValidationReport` 新增 `markdownReport` 和 `markdownLines`，可输出本地图文向量技术验证 Markdown 报告。
- 报告包含模型版本、来源、许可证、验证状态、样本数量、语言覆盖、缺失语言、逐样本维度与相似度分数、失败原因、结论与限制说明。
- 报告明确写入“未通过时不能标记视觉语义搜索完成”，并说明验证过程应在本机运行，不上传图片、OCR 文本或向量。
- Markdown 表格单元现在会转义竖线和换行，避免真实查询文本破坏报告结构。
- 新增 XCTest 覆盖通过报告、失败报告和 Markdown 表格转义。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，30 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `IndexStatusSummary` 新增 `embeddingOutdated`，用于在当前模型版本变化时统计已经 ready 但 `modelVersion` 不匹配的旧图片向量。
- `IndexStore.summary(currentEmbeddingModelVersion:)` 现在可以按当前 embedding 模型版本生成状态摘要；旧模型向量会显示为“需重建”，不会继续混在当前模型的 ready 数量里。
- 视觉索引状态 UI 会在向量任务中展示“需重建”数量；如果没有 pending 但存在旧模型向量，“开始视觉索引”仍可启动重建。
- 更新 XCTest，验证旧模型版本的 ready 向量在新模型版本下会计入 `embeddingOutdated`，并会进入 `fetchEmbeddingCandidates` 的重建候选。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`。该改动只确保未来真实模型版本变化时不会把旧向量误报为当前模型已完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，37 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingValidationSampleDescriptorDocument`，支持从 JSON 样本数组或 `{ "samples": [...] }` 文档解析真实 Photos 验证样本描述。
- 新增 `EmbeddingValidationSampleDescriptorAudit`，在读取真实图片前检查样本数量、中文/英文/中英文语言覆盖、空字段、重复样本 ID 和相关/无关查询是否相同。
- 审计摘要只输出样本 ID、数量、语言覆盖和问题原因，不输出 `assetLocalIdentifier`，避免把私有 Photos 资产标识写入报告或日志。
- 新增 `docs/视觉模型验证样本.example.json`，作为真实模型接入后手工填写 5 张 Photos 验证样本的模板。
- 更新 `docs/模型接入与验证指南.md`，说明样本描述 JSON 支持的两种格式、审计内容和隐私边界。
- 新增 XCTest 覆盖样本 JSON 文档解析、语言覆盖审计、重复样本 ID 检测和审计摘要不泄露私有资产 ID。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`。真实模型、真实 tokenizer 和 5 张真实 Photos 样本相似度验证仍未完成，真实视觉语义搜索不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，工程文件和 entitlements 校验通过。曾尝试用 `plutil` 校验 JSON 模板，但 `plutil` 不适用于该 JSON 文件格式，已改用 `python3 -m json.tool docs/视觉模型验证样本.example.json` 校验，JSON 通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，37 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 扩展 `EmbeddingModelReadinessReport`，新增 `manifestIssue` 字段，用于把 `EmbeddingModelManifest.json` 缺失、解析失败或模板占位问题单独展示出来。
- `EmbeddingService.bundledOrUnconfigured` 现在不会静默吞掉 manifest 加载失败；缺少真实 manifest 时仍安全降级到 `local-clip-unconfigured`，但模型就绪报告会保留具体原因，便于真实模型接入时定位问题。
- 通过 manifest 初始化 `EmbeddingService` 时，如果 manifest 仍是模板或缺少文件大小等关键元数据，这些问题会进入模型就绪报告和恢复提示。
- 更新 XCTest 覆盖缺少 bundled manifest、模板 manifest 和手工 readiness report 的中文诊断输出。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，27 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingValidationSampleDescriptor`，用于记录真实 Photos 验证样本的样本 ID、`assetLocalIdentifier`、相关查询、无关查询和查询语言。
- 新增 `EmbeddingValidationSampleLoader`，可把样本描述符转换为 `EmbeddingValidationSample`，单张图片读取失败时记录样本 ID 和失败原因，不阻塞其他样本。
- `PhotoLibraryService` 新增 `loadEmbeddingValidationSamples`，通过 PhotoKit 根据 `assetLocalIdentifier` 读取验证样本图片，不直接访问 Photos Library 文件包。
- 验证报告仍只使用样本 ID，不写入 Photos 资产标识，避免把私有图库标识沉淀到报告中。
- 新增 XCTest 覆盖样本描述符校验、成功和失败样本混合加载，以及报告输入样本不会暴露 `assetLocalIdentifier`。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`。真实 5 张 Photos 样本验证链路具备结构化入口，但尚未在真实模型下完成验证，真实视觉语义搜索仍不能标记为完成。
- 首次构建发现 `EmbeddingQueryLanguage` 未声明 `Codable`，导致样本描述符无法合成编解码；已按构建错误补齐，没有移除样本描述符可持久化要求。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，34 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增本地 JSON BPE tokenizer 适配层，可从 bundle 中的 tokenizer JSON 读取词表、BPE merge 规则、起始 token、结束 token、unknown token 和大小写规则。
- `EmbeddingService.bundledOrUnconfigured` 现在会在读取真实 `EmbeddingModelManifest.json` 后尝试自动加载 `configuration.tokenizerResourceName` 指向的 tokenizer；如果 tokenizer JSON 解析失败，会写入模型就绪诊断。
- `EmbeddingModelConfiguration` 现在要求 manifest 明确提供 tokenizer 资源名，避免文本编码器存在但文本 token 生成规则缺失时被误判为可验收。
- 新增 XCTest 覆盖本地 JSON BPE tokenizer 的基础 merge 行为，以及 bundled manifest 路径下 tokenizer 解析失败必须进入模型就绪报告。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`。本地 JSON BPE tokenizer 适配层尚未用真实 CLIP 类文本编码器验证，真实视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，32 个 XCTest 全部通过。

## 2026-06-27

- 根据真实使用反馈继续优化照片浏览区。
- 将照片卡片的创建时间、媒体类型和尺寸信息移入缩略图底部的信息条，减少每个网格单元格的纵向高度，让同一窗口中可以显示更多行图片。
- 收紧主界面外边距、左侧状态栏宽度和照片工具栏高度，并提高右侧照片工作区布局优先级。
- 本次只修改 SwiftUI 布局，不改变 PhotoKit 读取、Photos 只读边界、OCR 管线、索引数据结构或视觉模型能力；点击图片放大预览仍属于后续搜索界面和图片预览里程碑。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，34 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingModelReadinessReport` 新增模型接入清单字段，真实 manifest 提供的模型版本、来源、许可证、文件大小、输入输出名和预处理信息可以随就绪报告传递。
- `EmbeddingValidationReport` 的 Markdown 报告新增“模型接入清单”和“模型就绪诊断”章节，避免真实样本报告只记录相似度而缺少模型来源、许可证、资源状态和配置诊断。
- 更新 XCTest，验证技术验证报告会包含模型接入清单和就绪诊断。
- 新增 `docs/模型接入与验证指南.md`，用中文记录真实 Core ML 图文双塔模型接入前必须确认的来源、许可证、替代方案、影响、manifest 字段、5 张样本验证标准和不能标记通过的情况。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索仍不能标记为完成。
- 首次构建发现 `EmbeddingValidation.swift` 中 `lines.append(contentsOf:)` 少一个右括号，已按构建错误定位并修复，没有通过删除报告要求绕过问题。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，30 个 XCTest 全部通过。

## 2026-06-27

- 根据真实使用反馈继续优化主界面，让照片浏览区成为授权成功后的主要工作区域。
- 将主窗口最小尺寸调整为更适合左右工作区的比例，左侧状态栏宽度收紧到 310pt，右侧照片网格占据完整剩余宽度和高度。
- 授权说明卡改为更紧凑的字号与间距，保留 Photos 只读和本机处理提示。
- 本地索引卡默认只展示记录同步摘要和 OCR 状态摘要；OCR 操作、视觉索引诊断、开始/重试按钮折叠到“OCR 与视觉索引操作”详情区，减少对照片网格的视觉干扰。
- 微调照片网格的自适应列宽、行距和标题栏字号，让同一屏可以稳定看到更多照片行。
- 本次只修改 SwiftUI 布局，不改变 PhotoKit 读取、Photos 只读边界、OCR 管线、索引数据结构或视觉模型能力。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，27 个 XCTest 全部通过。

## 2026-06-27

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 扩展 `EmbeddingModelManifest` 元数据，新增图片模型、文本模型和 tokenizer 的文件大小字段，用于满足技术验证中“记录模型文件大小”的要求。
- manifest 校验现在会在图片模型、文本模型或 tokenizer 文件大小缺失时报告配置问题，防止只有模型文件名而没有体积记录的半成品配置进入验收。
- 新增 `requiredResourceNames` 和 `integrationChecklistLines`，可输出真实模型接入清单，包括模型版本、来源、许可证、资源文件名、文件大小、输入输出名、图片输入尺寸和文本序列长度。
- 更新 `EmbeddingModelManifest.example.json`，保留文件大小为 `null`，明确模板仍会被校验拒绝，不能作为真实模型使用。
- 新增和更新 XCTest，覆盖真实 manifest 文件大小解析、资源清单、接入清单，以及模板缺少文件大小时的失败提示。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索仍不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，27 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingModelPackagedResource` 新增 manifest 建议字段生成能力，会根据已打包资源的实际大小和 SHA-256 输出可复制的 manifest 片段。
- `EmbeddingModelReadinessReport` 汇总图片模型、文本模型和 tokenizer 的建议字段，便于真实模型资源加入后把实测大小与哈希写回 `EmbeddingModelManifest.json`。
- 视觉语义索引详情 UI 新增“manifest 建议字段”区域，字段可选中复制；该区域只显示本地资源元数据，不代表模型来源、许可证、tokenizer 匹配性或语义效果已经通过。
- 新增 XCTest 覆盖 manifest 建议字段中的文件大小和 SHA-256 输出。
- 更新 `README.md`、`docs/已知问题.md`、`docs/模型接入与验证指南.md` 和 `docs/验收记录.md`，明确该能力是模型接入辅助，不把真实视觉语义搜索标记为完成。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实 5 张 Photos 样本验证和中英文语义效果仍未完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，49 个 XCTest 全部通过。

## 2026-06-28

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingModelPackageInspector` 和 `EmbeddingModelPackageReport`，可对本机候选模型包目录执行预检，解析 `EmbeddingModelManifest.json`，检查图片模型、文本模型和 tokenizer 是否存在，并复用大小与 SHA-256 审计逻辑。
- 预检报告会输出资源清单、manifest 建议字段和 Markdown 报告，方便真实模型加入工程前先确认本地资源元数据。
- 预检报告明确说明只验证资源存在性、manifest 元数据、文件大小和 SHA-256，不证明 `.mlmodelc` 能被 Core ML 成功加载，也不证明图文向量语义空间已经对齐。
- 新增 XCTest 覆盖缺少 manifest 的恢复提示，以及候选模型包资源完整时的清单、建议字段和 Markdown 报告输出。
- 更新 `README.md`、`docs/已知问题.md`、`docs/模型接入与验证指南.md` 和 `docs/验收记录.md`，继续明确该能力不能替代真实 5 张 Photos 样本技术验证。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索仍不能标记为完成。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，51 个 XCTest 全部通过。

## 2026-06-28

- 根据真实使用反馈继续优化照片浏览区域，目标是在授权成功和 OCR 状态可见的同时，让照片网格成为主要可视区域。
- 将授权成功后的左侧状态栏进一步收紧到 260pt，减少主界面外边距和状态栏纵向间距。
- 将照片网格自适应列宽收紧到 92-124pt，行距收紧到 8pt，并降低缩略图底部信息条高度，让同一窗口中可以看到更多行图片。
- 收紧本地索引卡默认摘要的字号、间距和内边距；OCR、视觉索引和技术验证操作仍默认折叠在详情区。
- 本次只修改 SwiftUI 布局和中文文档，不改变 PhotoKit 读取、Photos 只读边界、OCR 管线、索引数据结构或视觉模型能力；点击图片放大预览仍属于后续搜索界面和图片预览里程碑。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，51 个 XCTest 全部通过。

## 2026-06-29

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 修正本地模型包预检报告文案，明确预检不再只是资源元数据检查，还会尝试加载候选 `.mlmodelc`，并检查 manifest 声明的输入输出接口是否能被当前 Core ML 推理管线使用。
- 更新 XCTest，要求预检 Markdown 报告说明会尝试加载 Core ML 模型，并说明不会运行图片编码、文本编码或相似度比较。
- 更新 `README.md`、`docs/已知问题.md`、`docs/模型接入与验证指南.md` 和 `docs/验收记录.md`，避免把预检能力误写成真实视觉语义搜索已完成。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实 5 张 Photos 样本验证和中英文语义效果仍未完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，51 个 XCTest 全部通过。

## 2026-06-29

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 技术验证报告新增最小相似度差距和每个样本的实际相似度差距，Markdown 样本表新增“相似度差距”列。
- `EmbeddingValidationService` 现在会在相关查询分数没有超过无关查询分数加 margin 时，写出当前差距和需要大于的阈值，避免真实模型验证时只看到模糊的“未通过”。
- 新增 XCTest 覆盖 margin 不足的失败场景，并更新报告表头、样本行和 summary 的断言。
- 更新 `README.md`、`docs/已知问题.md`、`docs/模型接入与验证指南.md` 和 `docs/验收记录.md`，明确相似度差距只是技术验证记录，不能替代真实模型和真实 Photos 样本验收。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实 5 张 Photos 样本验证和中英文语义效果仍未完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，52 个 XCTest 全部通过。

## 2026-06-29

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingInferenceExecutionPolicy`，明确图片向量和文本向量 Core ML 推理应在后台 `utility` 任务中执行，并提供主线程执行诊断文案。
- `EmbeddingService.encodeImage` 和 `EmbeddingService.encodeText` 继续使用 `Task.detached` 执行模型加载、预处理和推理，现在统一引用执行策略，避免后续真实模型接入时把推理误放到主线程。
- 新增 XCTest 覆盖执行策略的 `utility` 优先级和主线程诊断文案。
- 更新 `README.md`、`docs/决策记录.md`、`docs/已知问题.md` 和 `docs/验收记录.md`，说明该能力只降低界面阻塞风险，不能替代真实模型和真实 Photos 样本验证。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实 5 张 Photos 样本验证和中英文语义效果仍未完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，53 个 XCTest 全部通过。

## 2026-06-29

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingModelRuntimeAudit` 在候选 `.mlmodelc` 可加载后，除检查输入输出名和 `MLMultiArray` 类型外，新增输入 shape 与数据类型校验。
- 图片编码器输入必须与当前预处理管线一致：`float32`，shape 为 `[1, 3, imageInputSize, imageInputSize]`。
- 文本编码器输入必须与当前 token 构造一致：`int32`，shape 为 `[1, textSequenceLength]`。
- 预检 Markdown 报告、`README.md`、`docs/已知问题.md`、`docs/验收记录.md`、`docs/模型接入与验证指南.md` 和 `docs/决策记录.md` 已同步说明该能力。
- 本次没有加入真实 Core ML 模型、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实 5 张 Photos 样本验证、中英文语义效果和真实模型耗时仍未完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，54 个 XCTest 全部通过。

## 2026-06-29

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `docs/视觉模型候选评估.md`，记录 OpenAI CLIP、Apple MobileCLIP / MobileCLIP2 和 OpenCLIP 生态模型的来源、许可证风险、替代方案和影响。
- 当前评估结论：优先评估 OpenAI CLIP 或同等 CLIP 类模型的本地 Core ML 转换产物；Apple MobileCLIP / MobileCLIP2 因官方模型权重条款限定 Research Purposes，暂不作为默认内置模型；OpenCLIP 生态模型需要逐个确认具体权重许可证。
- 更新 `README.md`、`docs/已知问题.md`、`docs/验收记录.md` 和 `docs/决策记录.md`，说明该评估只是接入决策依据，不包含模型文件，也不能替代真实样本技术验证。
- 本次没有加入真实 Core ML 模型、转换脚本、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实 5 张 Photos 样本验证、中英文语义效果和真实模型耗时仍未完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，54 个 XCTest 全部通过。

## 2026-06-29

- 根据当前 token 和开发收益重新收敛第 4 里程碑范围：本阶段只保留让后续链路可走通的最小视觉向量基础设施，不继续扩展未验证的模型能力。
- 当前最佳完成边界是：没有真实本地 Core ML 图文双塔模型时，App 必须显示模型未就绪，不生成伪视觉语义结果；加入真实模型后，可沿用 manifest、模型预检、技术验证、向量持久化和视觉查询入口继续推进。
- `EmbeddingModelManifest` 新增外部模型引入原因、替代方案和影响评估字段；缺少这些字段时模型会保持不可用，避免未说明来源和代价的模型进入验收。
- 更新 `EmbeddingModelManifest.example.json`、README、已知问题、验收记录和相关 XCTest，明确真实 manifest 必须写明引入原因、替代方案、影响评估、许可证、文件大小、SHA-256、tokenizer 和预处理参数。
- 本次没有加入真实 Core ML 模型、转换脚本、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索不能标记为完成。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，54 个 XCTest 全部通过。

## 2026-06-29

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingModelConfiguration` 新增 `textStartTokenID` 和 `textEndTokenID`，真实 manifest 需要记录文本编码器使用的起始和结束 token ID。
- 本地 JSON BPE tokenizer 加载后会比对词表中的起止 token ID 与 manifest 记录；不一致时进入模型就绪诊断，避免文本编码链路能运行但语义空间实际不匹配。
- 更新 `EmbeddingModelManifest.example.json`、`README.md`、`docs/模型接入与验证指南.md`、`docs/已知问题.md` 和 `docs/验收记录.md`，说明起止 token ID 是真实模型接入合同的一部分。
- 新增 XCTest 覆盖 tokenizer 起止 token 与 manifest 不一致时的诊断。
- 本次没有加入真实 Core ML 模型、转换脚本、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实 5 张 Photos 样本验证、中英文语义效果和真实模型耗时仍未完成。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，55 个 XCTest 全部通过。

## 2026-06-29

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- 新增 `EmbeddingValidationGate`，要求当前模型版本的技术验证报告通过后，才允许执行视觉索引或视觉查询。
- `PhotoLibraryViewModel` 在启动视觉索引和视觉查询前会检查该门禁；未运行验证、验证失败或报告模型版本不匹配时，会显示恢复提示，不会生成视觉向量，也不会返回视觉候选。
- 该改动落实了 PLAN 中“先完成本地 Core ML 图文模型技术验证，验证通过后再接入完整视觉索引流程”的顺序，避免仅凭模型资源预检就污染本地索引。
- 新增 XCTest 覆盖无技术验证报告、模型版本不一致、验证失败和验证通过四种门禁状态。
- 同步更新 `README.md`、`docs/已知问题.md`、`docs/验收记录.md` 和 `docs/决策记录.md`。
- 本次没有加入真实 Core ML 模型、转换脚本、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实 5 张 Photos 样本验证、中英文语义效果和真实模型耗时仍未完成。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，56 个 XCTest 全部通过。

## 2026-06-29

- 根据当前开发成本和里程碑 4 的最小可交付目标，进一步收敛实现范围：保留后续链路需要的视觉索引、视觉查询、向量持久化和验证门禁，不继续扩展未验证的真实模型能力。
- 新增 `EmbeddingValidationSummary`，技术验证通过后除完整 Markdown 报告外，还会在本机 Application Support 的 `PictureSearch/EmbeddingValidationSummary.json` 保存最小验证摘要。
- `PhotoLibraryViewModel` 启动时会读取验证摘要；同一模型版本且摘要为通过状态时，可恢复视觉索引和视觉查询门禁，避免 App 重启后必须重新运行技术验证。
- 验证摘要只保存模型版本、来源、许可证、样本数量、语言覆盖、相似度阈值、平均耗时和通过状态，不保存图片内容或 Photos 私有资产标识。
- 如果摘要缺失、损坏、模型版本不一致或验证失败，视觉索引和视觉查询仍会被阻止，不生成伪视觉结果。
- 更新 `README.md`、`docs/决策记录.md`、`docs/已知问题.md`、`docs/模型接入与验证指南.md` 和 `docs/验收记录.md`，明确当前只是最小链路和门禁能力完成，真实 Core ML 图文模型、匹配 tokenizer、5 张真实 Photos 样本验证和中英文语义效果仍未完成。
- 本次没有加入真实 Core ML 模型、转换脚本、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实视觉语义搜索不能标记为完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，56 个 XCTest 全部通过。

## 2026-06-29

- 继续推进第 4 里程碑：本地图文向量与视觉语义搜索。
- `EmbeddingService` 新增本机模型包加载路径，App 启动时优先检查 Application Support 下的 `PictureSearch/EmbeddingModelPackage`，该目录不存在时再回退到 App bundle 资源。
- 本机模型包目录需要包含 `EmbeddingModelManifest.json`、图片编码器 `.mlmodelc`、文本编码器 `.mlmodelc` 和 manifest 声明的 tokenizer；仍会执行 manifest 元数据、许可证、引入原因、替代方案、影响评估、文件大小和 SHA-256 校验。
- `EmbeddingService.modelReadinessReport()` 现在也会尝试加载候选 `.mlmodelc`，并检查输入输出名、输入 shape 和数据类型，避免无效 Core ML 编译目录被误判为可用。
- `PhotoLibraryViewModel` 默认使用 `EmbeddingService.localPackageOrBundledOrUnconfigured()`，真实模型接入不再必须修改 Xcode bundle 资源。
- 新增 XCTest 覆盖本机模型包目录加载、tokenizer 读取、资源哈希匹配，以及无效 `.mlmodelc` 被运行时就绪检查阻止。
- 更新 `README.md`、`docs/模型接入与验证指南.md`、`docs/已知问题.md`、`docs/决策记录.md` 和 `docs/验收记录.md`。
- 本次没有加入真实 Core ML 模型、转换脚本、外部依赖或网络能力；当前模型版本仍为 `local-clip-unconfigured`，真实 5 张 Photos 样本验证、中英文语义效果和真实模型耗时仍未完成。
- 已运行 `plutil -lint PictureSearch.xcodeproj/project.pbxproj PictureSearch/PictureSearch.entitlements`，校验通过。
- 已运行 `python3 -m json.tool PictureSearch/Resources/EmbeddingModelManifest.example.json`，manifest 模板 JSON 校验通过。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，57 个 XCTest 全部通过。

## 2026-06-29

- 根据当前 token 成本和产品可用性目标，正式收敛后续路线：第 4 里程碑不再继续追求完整真实 Core ML CLIP 类视觉语义搜索，改为以“视觉能力预留、模型验证门禁和安全降级”收口。
- 已更新 `PLAN.md`：第 5、6、7 里程碑改为优先交付 MVP，核心能力为本地 OCR 图片搜索、时间/类型解析、基础融合排序、结果解释、大图预览、复用入口、手动刷新、失败重试和清除重建。
- 视觉语义搜索被调整为后续增强能力：只有在合法模型、匹配 tokenizer、预处理配置、5 张真实 Photos 样本验证、中英文查询效果和技术验证报告全部通过后，才允许启用视觉索引和视觉查询。
- MVP 阶段不会用人工标签、OCR 文本、文件名或基础元数据冒充视觉语义搜索；视觉未启用时必须明确降级，不能返回伪视觉候选。
- 本次只修改计划和说明文档，没有修改应用代码、没有下载模型、没有新增外部依赖，也没有运行构建或测试。

## 2026-06-29

- 按新版 `PLAN.md` 完成第 4 里程碑收口：目标定义为本地图文向量预留、模型验证门禁和安全降级，不再把真实 CLIP 类视觉语义搜索作为 MVP 前置条件。
- 更新 `docs/里程碑4当前审计.md` 和 `docs/验收记录.md`，将第 4 里程碑状态从旧口径的“部分完成”调整为“MVP 自动验收通过，真实视觉语义搜索增强项未验收”。
- 完成证据：未配置或未验证模型时，视觉索引和视觉查询会被模型就绪检查与 `EmbeddingValidationGate` 阻止；App 不下载模型、不上传图片、不使用人工标签、OCR 文本、文件名或基础元数据冒充视觉语义搜索。
- 已运行 `plutil -lint code/PictureSearch.xcodeproj/project.pbxproj code/PictureSearch/PictureSearch.entitlements`，工程文件和 entitlements 校验通过。
- 已运行 `python3 -m json.tool code/PictureSearch/Resources/EmbeddingModelManifest.example.json` 和 `python3 -m json.tool code/docs/视觉模型验证样本.example.json`，两个 JSON 模板均可解析。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project code/PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，57 个 XCTest 全部通过。

## 2026-06-29

- 执行第 5 里程碑：查询解析、OCR 检索和融合排序。
- 扩展 `SearchModels.swift`，新增搜索命中类型、结果置信度、结构化命中原因、查询计划和基础资产类型模型。
- 扩展 `SearchService`：支持解析“最近”“去年”“去年夏天”“2025 年 10 月”等时间表达，识别“截图”“照片”“文档图”等基础类型线索，按 OCR 文本进行关键词匹配，并将 OCR、时间和类型信号融合成统一分数。
- 排序策略：OCR 精确命中优先；多信号同时匹配会获得高置信；单一时间或类型弱匹配标记为相近结果；纯画面描述在真实视觉模型未验证时明确降级，不生成伪视觉结果。
- 主界面新增“本地搜索”面板，可输入自然语言查询并展示前 5 条结果摘要、置信度、分数、时间尺寸和命中原因；大图预览和完整结果网格仍属于第 6 里程碑。
- 新增 XCTest 覆盖时间解析、类型解析、OCR 检索、融合排序、低置信结果、命中解释和视觉不可用降级。
- 更新 `README.md`、`docs/已知问题.md`、`docs/验收记录.md` 和本开发记录，说明第 5 里程碑完成范围、限制和人工检查项。
- 已运行 `/Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project code/PictureSearch.xcodeproj -scheme PictureSearch -destination 'platform=macOS' -derivedDataPath /private/tmp/picture_search_derived build`，构建通过。
- 已运行 `xcodebuild test` 构建测试 bundle；运行阶段仍受当前沙盒 `testmanagerd.control` 限制，错误为无法与 test runner 建立通信。
- 已使用 `DYLD_LIBRARY_PATH=/private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/MacOS /Applications/Xcode.app/Contents/Developer/usr/bin/xctest /private/tmp/picture_search_test_derived/Build/Products/Debug/PictureSearch.app/Contents/PlugIns/PictureSearchTests.xctest` 直接运行测试 bundle，61 个 XCTest 全部通过。
